import { EntityDirectiveDirective } from './entity-directive.directive';

describe('EntityDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
