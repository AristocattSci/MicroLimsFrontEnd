import { EntityViewerDirective } from './entity-viewer.directive';

describe('EntityViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityViewerDirective();
    expect(directive).toBeTruthy();
  });
});
